function [trainMatrix, testMatrix, validation] = TrainTestSplit(matrix, alpha)
    validation = matrix(1,:);
    matrix(1,:) = [];
    test_size = floor(alpha * (size(matrix, 1) - 1));
    testMatrix = matrix(1:test_size,:);
    [X, Y] = SplitIntoXY(testMatrix, deltaTp, deltaTr);
    trainMatrix = matrix(test_size+1:end, :);
end

X = matrix(:, 1:deltaTp);
    Y = matrix(:, deltaTp + 1:obj_len);
    x = X(1, :);
    X(1, :) = [];
    y = Y(1, :);
    Y(1, :) = [];